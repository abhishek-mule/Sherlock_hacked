# APK

Place `app-release-signed.apk` here after `scripts/build-apk.sh` or GitHub Release.

Users then download via `/download` or direct `/apk/app-release-signed.apk`.

Keep APKs out of git LFS if large — prefer GitHub Releases (`gh release upload`).
